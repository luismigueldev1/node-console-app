# Aplicacion de tablas de multiplicar.

## Como usar:

```
node app [options]

```

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Representa la base de la multiplicación     [number] [required]
  -l, --listar   Lista la tabla de multiplicar        [boolean] [default: false]
  -h, --hasta    Representa el numero hasta donde se mulplicara la base
                                                          [number] [default: 10]
```
